language: scala

jdk: oraclejdk8

before_install:
- curl -sL -o consul.zip https://dl.bintray.com/mitchellh/consul/0.5.2_linux_amd64.zip
- unzip consul.zip
- nohup ./consul agent -server -bootstrap -data-dir=/tmp
